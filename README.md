# Basic Node.js Server

A simple basic Node.js server using the built-in `http` module.

## Requirements

- Node.js installed

Check version:

```
node -v

```
node app.js 

Server url 
```
http://localhost:3000
